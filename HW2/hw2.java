package dataStructures.HW2;
import java.util.ArrayList;

public class hw2 {
   public static void main(String[] args){
      ArrayList<Integer> arr = new ArrayList<>();
      arr.add((int) (Math.random()*50)+0);
      arr.add((int) (Math.random()*50)+0);
      arr.add((int) (Math.random()*50)+0);
      arr.add((int) (Math.random()*50)+0);
      arr.add((int) (Math.random()*50)+0);
     // min(arr);

   }
   //public static <E extends Comparable<E>> E min(E [] list) {

   //}
}
